使用桥接模式:
分离抽象与具体实现,让他们可以独自发展.
Gift -> WarmGift ColdGift WildGift  抽象继承树
GiftImpl -> Flower Ring Car  实现继承树

抽象继承树  和 实现继承树 分离开让其各自发展, 让 抽象中聚合一个具体实现.

也就是说每个 Gift 中都有个 Gift 的实现.
每一个Gift 在new 出的时候都要 聚合一个 具体的实现. 这样就可以两个维度 同时发展不会造成影响.
